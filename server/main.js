import { Meteor } from 'meteor/meteor';
import '../imports/server/messages/messages.methods';
import '../imports/server/messages/messages.publish'

Meteor.startup(() => {
  // code to run on server at startup
});
